projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: https://habit-tracker-app-layer.herokuapp.com/
  database:
    schemaPath: src/generated/prisma.graphql
    extensions:
      prisma: database/prisma.yml
